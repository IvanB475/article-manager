export interface EnvironmentVariablesInterface {
    db_host: string,
    db_port: number,
    db_database: string,
    db_username: string,
    db_password: string,
    api_port: number;
    token_secret: string;
}
